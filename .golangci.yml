linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - gofmt
    - goimports
    - gosec

linters-settings:
  goimports:
    local-prefixes: github.com/iszlai/chamele-go

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gosec
